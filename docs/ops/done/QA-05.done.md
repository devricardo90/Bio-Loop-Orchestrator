# [DONE] QA-05 Release gate pos-M7 para auth real + buyer real-data

## O que foi entregue

- gate pos-M7 consolidado em `docs/ops/POST_M7_RELEASE_GATE.md`
- cobertura Playwright atualizada para refletir o texto real da UI apos `WEB-13`
- buyer auth/browser checks endurecidos com validacao de `source=api`, acesso ao `/reference` e handoff de sessao expirada

## Gate executado

- `pnpm.cmd typecheck`: PASS
- `pnpm.cmd --filter @bio-loop/web test`: PASS
- `pnpm.cmd --filter @bio-loop/api test`: PASS
- `pnpm.cmd test:e2e`: FAIL (`spawn EPERM` no ambiente local ao iniciar Playwright)

## Resultado pratico

- o projeto agora tem um release gate unico para auth real, buyer real-data, seller/admin review e docs vivas
- a suite browser cobre explicitamente os sinais que diferenciam o caminho real da API do fallback antigo
- o unico bloqueio remanescente neste ambiente e operacional do Playwright no Windows (`spawn EPERM`), nao uma regressao de produto
