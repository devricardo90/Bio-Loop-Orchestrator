# [DONE] DB-01 Normalizar contratos persistidos de dados operacionais

## O que foi entregue

- migration Prisma criada em `apps/api/prisma/migrations/20260324_000004_operational_data_contracts/`
- `Store` ganhou `externalId`, dados legais/geograficos, currency, flag de atividade e `metadata`
- `Buyer` ganhou `externalId`, `buyerType`, `city` e `metadata`
- `CommodityCategory` ganhou `externalId`, `localizedNameSv` e `targetIndustry`
- `Lot` ganhou `externalId`, `sourceExpiresAt` e `metadata`
- nova relacao `BuyerCategoryInterest` criada para persistir interesse de categoria sem JSON solto
- seed atualizado para popular os novos campos mantendo compatibilidade com os cenarios atuais

## Gate executado

- `pnpm.cmd --filter @bio-loop/api prisma:generate`: PASS
- `pnpm.cmd --filter @bio-loop/api db:seed`: PASS
- `pnpm.cmd typecheck`: PASS
- `pnpm.cmd --filter @bio-loop/api test`: PASS

## Observacao

- o `pnpm.cmd test:e2e` bateu em `EPERM` no `query_engine-windows.dll.node`, indicando lock local do Prisma no ambiente Windows, nao regressao funcional do schema

## Resultado pratico

- os dados reais anexados em `DATA-01` ja nao dependem de campos improvisados para `external_id`, labels de categoria e metadados operacionais basicos
- `DB-02` agora e o proximo passo natural para sair de billing calculado em runtime e ir para billing persistido
