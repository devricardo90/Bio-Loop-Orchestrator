# [DONE] DB-04 Read models e indices para queries reais

## O que foi entregue

- migration de indices para buyer, auction, order, invoice e billing export
- narrowing de `select` no buyer read-model para evitar overfetch
- narrowing de `select` em admin buyers e billing para reduzir carga de query
- teste dedicado para garantir indices/read-models no schema e nos services

## Gate executado

- `pnpm.cmd --filter @bio-loop/api prisma:generate`: PASS
- `pnpm.cmd typecheck`: PASS
- `pnpm.cmd --filter @bio-loop/api test`: PASS
- `pnpm.cmd --filter @bio-loop/web test`: PASS
- `pnpm.cmd exec prisma migrate deploy --schema prisma/schema.prisma` em Postgres isolado: PASS
- `node prisma/seed.mjs` em Postgres isolado: PASS

## Resultado pratico

- buyer/admin/billing passam a declarar melhor os campos realmente carregados
- queries criticas agora tem indices explicitos para leitura e sweeps principais
- a proxima etapa correta da fase M9 passa a ser `API-14`
