# [DONE] DB-03 Seed v2 idempotente e orientado a cenarios

## O que foi entregue

- substituicao do seed destrutivo por um catalogo de cenarios gerenciados por IDs estaveis
- limpeza direcionada de IDs do seed atual e do seed legacy, sem apagar tabelas inteiras fora do escopo do projeto
- cenarios baseline cobrindo:
  - buyer live auction (`auction-husks-01`)
  - scheduled auction (`auction-apples-01`)
  - awarded pickup flow (`order-carrots-01`)
  - void auction (`auction-greens-01`)
  - settled billing + resolved dispute (`order-beets-01`)
  - open no-show dispute para admin (`order-roots-01`)
- alinhamento do seed com IDs e rotas que ja aparecem na web e na suite e2e

## Gate esperado

- `pnpm.cmd --filter @bio-loop/api prisma:generate`
- `pnpm.cmd --filter @bio-loop/api db:seed`
- validacao de que o seed roda tanto em banco limpo quanto em banco previamente inicializado com IDs legacy

## Impacto operacional

- `DATA-01` fica destravada como proxima task para onboarding dos dados reais dos supermercados da Suecia
- `API-12` e `INFRA-05` tambem ficam tecnicamente prontas para fila, mas a ordem manual segue pelo primeiro `READY`
