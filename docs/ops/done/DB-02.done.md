# [DONE] DB-02 Persistencia real de billing e invoice

## O que foi entregue

- migration Prisma criada em `apps/api/prisma/migrations/20260324_000005_billing_persistence/`
- novas entidades persistidas: `Invoice`, `InvoiceFee` e `BillingExport`
- `Order` agora referencia `Invoice`
- `BillingService` passou a materializar invoices persistidas a partir de `Order SETTLED`
- export agora persiste snapshot em `BillingExport` e atualiza `Invoice.status/exportedAt`
- seed atualizado com invoice e fees persistidos para o cenario settled
- teste de billing adaptado para o novo caminho persistido

## Gate executado

- `pnpm.cmd --filter @bio-loop/api prisma:generate`: PASS
- `pnpm.cmd --filter @bio-loop/api db:seed`: PASS
- `pnpm.cmd typecheck`: PASS
- `pnpm.cmd --filter @bio-loop/api test`: PASS
- `pnpm.cmd test:e2e`: PASS

## Resultado pratico

- seller reports continua compativel com a UI atual
- fees e export deixam de existir apenas em memoria de request
- billing fica preparado para evolucao de reconciliation, settlement e auditoria posterior
