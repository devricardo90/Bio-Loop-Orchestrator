{
  "name": "@bio-loop/shared",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node test/smoke.test.mjs"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.1.0"
  }
}
