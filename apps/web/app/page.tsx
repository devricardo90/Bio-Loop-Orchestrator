export default function HomePage() {
  return (
    <main className="page-shell">
      <section className="hero-card">
        <p className="eyebrow">Bio-Loop-Orchestrator</p>
        <h1>Monorepo foundation is in place.</h1>
        <p className="lead">
          The API, web app, and shared packages now have a minimal workspace
          scaffold ready for domain contracts.
        </p>
      </section>
    </main>
  );
}

