import { LoginPanel } from "../../components/login-panel";

export const metadata = {
  title: "Login",
  description: "Seller and buyer authentication for the Bio Loop workspace"
};

export default function LoginPage() {
  return <LoginPanel />;
}
